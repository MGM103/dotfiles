# File system
alias ls='eza -lh --group-directories-first --icons=auto'
alias lsa='ls -a'
alias lt='eza --tree --level=2 --long --icons --git'
alias lta='lt -a'

# Zoxide smart file jumping
zd() {
  if [ $# -eq 0 ]; then
    builtin cd ~ && return
  elif [ -d "$1" ]; then
    builtin cd "$1"
  else
    z "$@" && printf " \U000F17A9 " && pwd || echo "Error: Directory not found"
  fi
}

# Open command to open files or dirs in the explorer
open() {
  xdg-open "$@" >/dev/null 2>&1
  # /mnt/c/Windows/System32/cmd.exe /c start "$@" >/dev/null 2>&1
}

# Directories
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'

# Git
alias g='git'
alias gcm='git commit -m'

# tmux
alias tks='tmux kill-session -t'
alias tksvr='tmux kill-server'
alias tns='tmux new-session -ds'
alias tsw='tmux swap-window -t'

# Node shortcuts
alias changeset='npm run changeset'
alias ladle='npm run ladle'
alias lint='npm run lint:fix'
alias tc='npm run type-check'
alias vt='npm run visual-tests'

# Apps
alias oc='opencode'

# Projects
alias lcw='z lifechain-web'
alias lcui='z lifechain-ui'
alias lcww='z workflow-web'
alias lcll='z lifelink-web'
alias lcws='z lifechain-workflow-service'
alias lcds='z lifechain-device-service'
alias lcas='z tenant-admin-service'
